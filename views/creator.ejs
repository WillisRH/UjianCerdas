<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mapel Creator</title>
</head>
<body>
    <h1>Mapel Creator</h1>

    <% if (typeof mapelCreated !== 'undefined') { %>
        <h2>Mapel Created Successfully!</h2>
        <p>Mapel Name: <%= mapelCreated.mapel.mapelname %></p>
        <!-- You can display other mapel information here -->
    <% } %>

    <form action="/mapelcreate" method="POST">
        <label for="mapelname">Mapel Name:</label>
        <input type="text" id="mapelname" name="mapelname" required><br>
        <!-- You can add more input fields for other mapel properties here -->

        <button type="submit">Create Mapel</button>
    </form>
</body>
</html>
